// # copy paste project.json into the new projet's directory
// # copy the gulpfile.js into the new project's root
// # cd into the root of the project
npm install
// this will create node_modules/ and package-lock.json
gulp watch
Symfony Installer
	dedicated application
	PHP 5.4 compatible executable
	purpose: to ease the creation of Symfony applications



in your terminal
- cd you c:
- run the command:
c:\> php -r "readfile('https://symfony.com/installer');" > symfony

move the symfony file to a directory 
that`s included in the PATH environment variable 
to create the global command 
or move it to any other directory convenient for you:

c:\> move symfony c:\xampp\php\


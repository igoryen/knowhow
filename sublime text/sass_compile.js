Tools > Sass Builder Config 
A file opens.
Save the file:
    "Save as" >
    Pick location (the same folder where you store your .scss or .sass files)
    Name the config file ".sassbuilder-config".
Edit the configuration as needed.
    The path should be absolute: e.g. "c:/aaa/ig/webhub"